# chapter 8
# ex3 ---------------------------------------------------------------------
# P(Z <= 1.37), P(Z >= 1.37)
p1 <- pnorm(1.37)
p2 <- 1-pnorm(1.27)
p1
pnorm(1.37,0,1)
p2


# ex4 ---------------------------------------------------------------------

# P(Z < -0.155 or Z > 1.6)
p <- pnorm(-0.155) + 1-pnorm(1.6)
P

# ex5 ---------------------------------------------------------------------

# P(-z <= Z <= z) = 0.90
z <- -qnorm(0.05)
z
qnorm(0.5 + 0.45)

# ex6 ---------------------------------------------------------------------
# X ~ N(60, 16), P(55 <= X <= 63)
m = 60
sd = 4
p1 <- pnorm(63,m,sd)-pnorm(55,m,sd)
p1
pnorm((63-m)/sd)-pnorm((55-m)/sd)


# ex7 ---------------------------------------------------------------------
# N(63, 100)
# (1) 50점 이하인 학생은 몇퍼센트?
m = 63
sd = 10
pnorm(50,m,sd)
pnorm((50-m)/sd)

# (2) "상위 10% 학생에게 A학점, 몇점 이상이면 A학점?
z1 <- qnorm(1-0.1)
a1 <- m + sd*z1

a2 <- qnorm(1-0.1,m,sd)

c(a1,a2)
# ex8 ---------------------------------------------------------------------
# X ~ Bin(150, 0.6)일 때 다음 확률의 근사값?
# (1) P(82 <= X <= 101)
m = 150*0.6
sd = sqrt(150*0.6*0.4)
pnorm(101.5,m,sd) - pnorm(81.5,m,sd)

# (2) P(97 < X)
1-pnorm(97.5,m,sd)
1-pnorm((97.5-m)/sd)



# ex9 ---------------------------------------------------------------------
# 성인의 30%가 정기적으로 알코올성분 음료를 마신다.
# (1) 1000명 중 알코올성분 음료를 마시는 사람의 수가 280명 보다 작을 확률?
# P(X < 280) = P(X <= 279.5)
m = 1000*0.3
sd = sqrt(1000*0.3*0.7)
pnorm(279.5,m,sd)

# (2) 연속성 수정/수정 없이 계산하면
# P(X < 280) = P(X <= 279)
pnorm(279,m,sd)

# (3) 근사가 아닌 직접 계산 값 -> 연속성 수정을 한 것과 더 유사함!
pbinom(279,1000,0.3)
